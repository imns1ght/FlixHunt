import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    marginVertical: 10,
    marginTop: 20,
  },
  image: {
    height: 300,
    width: '100%',
    marginHorizontal: 5,
  },
})

export default styles
