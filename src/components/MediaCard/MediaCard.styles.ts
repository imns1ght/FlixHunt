import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  card: {
    width: 200,
    marginRight: 10,
  },
  disabledCard: {
    opacity: 0.5,
  },
  image: {
    height: 305,
    borderRadius: 5,
  },
})

export default styles
