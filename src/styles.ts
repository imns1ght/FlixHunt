const colors = {
  primary: '#BA181B',
  secondary: '#14141c',
  white: '#FFFFFF',
  black: '#000000',
  gray: '#D3D3D3',
}

const theme = {
  dark: true,
  colors: {
    primary: colors.primary,
    background: colors.secondary,
    card: colors.primary,
    text: colors.white,
    border: colors.gray,
    notification: colors.secondary,
  },
}

export { theme, colors }
