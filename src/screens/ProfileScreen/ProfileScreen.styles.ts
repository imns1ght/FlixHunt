import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  mainContainer: {
    flexGrow: 1,
  },
  listContainer: {
    marginTop: 30,
    flexGrow: 1,
    rowGap: 12,
  },
})

export default styles
